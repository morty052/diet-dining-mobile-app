import { StyleSheet, Text, View } from "react-native";
import React from "react";
import TinderScreen from "./discovery/Discovery";

type Props = {};

const TestScreen = (props: Props) => {
  return <TinderScreen />;
};

export default TestScreen;

const styles = StyleSheet.create({});
